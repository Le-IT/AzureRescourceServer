package com.leit.bzl.intranet.rescources.azurerescourceserver.azurerescourceserver;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class AzureRescourceServerApplication {

    public static void main(String[] args) {
        SpringApplication.run(AzureRescourceServerApplication.class, args);
    }

}
